import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'IonicProjectV6',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
